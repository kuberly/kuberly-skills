# Customer overlays

Optional **`.apm/skills/overlays/<tenant>/`** skills. Many teams keep overlays only inside the **customer fork** of kuberly-stack instead of this shared repo.

If you add overlays here, use Bitbucket **branch permissions** and **default reviewers** so tenant boundaries are enforced.
