# Shared references

Long-form documentation linked from **`SKILL.md`** files. Keep **`SKILL.md`** focused so agents load less context unless they open a reference.

- No secrets, credentials, or customer PII.
- Prefer stable section headings when linking from skills.
